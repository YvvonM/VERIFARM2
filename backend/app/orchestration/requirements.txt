# Orchestration layer (kept out of the runtime API image — install in the
# dagster service / a dedicated orchestrator image).
#   pip install -r app/orchestration/requirements.txt
dagster>=1.7
dagster-webserver>=1.7

# Optional secrets backends (install the one you use):
#   hvac>=2.0          # SECRETS_BACKEND=vault
#   boto3>=1.34        # SECRETS_BACKEND=aws
